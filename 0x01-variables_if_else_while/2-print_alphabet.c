#include <stdlib.h>
#include <stdio.h>
#include <time.h>
/* more headers goes there */

/* print numbers regardless the sign negative positive zero*/
int main(void) {
    
for( int l = 0; l < 26; l++ )
putchar( 'a' + l );
putchar( '\n' );

    return 0;
}
